<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OldYar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryDark">@color/purple</item>
        <item name="colorControlHighlight">@color/purple</item>
        <item name="fontFamily" type="font">@font/primary_bold</item>
    </style>

    <style name="Theme.OldYar" parent="Base.Theme.OldYar" />
    <item name="fontFamily" type="font">@font/primary_bold</item>
</resources>